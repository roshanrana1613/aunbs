import 'package:flutter/material.dart';

class TablatSettings extends StatelessWidget {
  const TablatSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Text("tablat settings"),
    );
  }
}
